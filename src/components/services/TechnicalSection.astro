
---
import type { ServicePage } from '../../types/service';

interface Props {
  technical: ServicePage['technical'];
}

const { technical } = Astro.props;
---
<section class="py-16 sm:py-20 bg-slate-50">
  <div class="mx-auto max-w-4xl px-6">
    <h2 class="text-3xl font-bold tracking-tight text-slate-900 text-center">
      {technical.title}
    </h2>
    <div class="mt-10">
      <dl class="space-y-6">
        {technical.highlights.map(highlight => (
          <div class="flex justify-between items-baseline">
            <dt class="text-base font-medium text-slate-600">{highlight.name}</dt>
            <dd class="text-lg font-semibold text-slate-900">{highlight.value}</dd>
          </div>
        ))}
      </dl>
    </div>
  </div>
</section>
