
---
import type { ServicePage } from '../../types/service';

interface Props {
  successMetrics: ServicePage['successMetrics'];
}

const { successMetrics } = Astro.props;
---
{successMetrics && (
  <section class="py-16 sm:py-20 bg-slate-50">
    <div class="mx-auto max-w-4xl px-6">
      <h2 class="text-3xl font-bold tracking-tight text-slate-900 text-center">
        {successMetrics.title}
      </h2>
      <div class="mt-12 grid grid-cols-1 sm:grid-cols-3 gap-8 text-center">
        {successMetrics.metrics.map(metric => (
          <div>
            <p class="text-5xl font-bold text-sky-600">{metric.value}</p>
            <p class="mt-2 text-base text-slate-600">{metric.label}</p>
          </div>
        ))}
      </div>
      {successMetrics.caseStudy && (
        <div class="mt-16">
          <blockquote class="text-xl italic text-slate-700 text-center">
            "{successMetrics.caseStudy.quote}"
          </blockquote>
          <p class="mt-4 text-base text-slate-500 text-center">
            - {successMetrics.caseStudy.client}
          </p>
        </div>
      )}
    </div>
  </section>
)}
