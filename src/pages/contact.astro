---
import Layout from '../layouts/Layout.astro';
const whatsappNumber = '6285165693179';
const whatsappMessage = encodeURIComponent('Halo kak saya mau coba konsultasi ai custom nih');
---

<Layout title="Hubungi Kami - my-aicustom.com">
  <!-- Hero Section -->
  <section class="relative bg-blue-700 text-white py-20 lg:py-28">
    <div class="absolute inset-0">
      <img src="https://images.unsplash.com/photo-1521737604893-d14cc237f11d?w=1200" alt="Tim konsultan AI" class="w-full h-full object-cover opacity-30" loading="lazy" />
    </div>
    <div class="container mx-auto px-6 max-w-screen-xl relative z-10 text-center">
      <span class="inline-flex items-center px-4 py-1 text-sm font-semibold uppercase tracking-wide bg-white/20 rounded-full">Konsultasi Gratis</span>
      <h1 class="mt-6 text-4xl lg:text-5xl font-extrabold">Mari Diskusikan Kebutuhan AI Anda</h1>
      <p class="mt-4 text-lg lg:text-xl text-blue-100">Isi formulir di bawah atau pilih kanal komunikasi favorit Anda. Tim kami akan merespons maksimal dalam 1x24 jam kerja.</p>
    </div>
  </section>

  <!-- Contact Options & Form -->
  <section class="py-20 lg:py-24 bg-white dark:bg-gray-900">
    <div class="container mx-auto px-6 max-w-screen-xl grid gap-16 lg:grid-cols-2">
      <!-- Contact Methods -->
      <div class="space-y-8">
        <div>
          <h2 class="text-3xl font-bold text-gray-900 dark:text-white">Hubungi secara langsung</h2>
          <p class="mt-3 text-gray-600 dark:text-gray-400">Pilih kanal yang paling nyaman bagi Anda untuk memulai percakapan.</p>
        </div>
        <div class="space-y-6">
          <div class="flex items-start gap-4 p-6 border border-gray-100 dark:border-gray-700 rounded-2xl shadow-sm">
            <span class="inline-flex items-center justify-center w-12 h-12 rounded-full bg-green-100 text-green-600">
              <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path d="M20 3H4a1 1 0 00-1 1v16l4-4h13a1 1 0 001-1V4a1 1 0 00-1-1z" /></svg>
            </span>
            <div>
              <h3 class="text-lg font-semibold text-gray-900 dark:text-white">WhatsApp Business</h3>
              <p class="mt-1 text-gray-600 dark:text-gray-400">Respons tercepat untuk konsultasi singkat dan koordinasi teknis.</p>
              <a href={`https://wa.me/${whatsappNumber}?text=${whatsappMessage}`} target="_blank" class="mt-3 inline-flex items-center text-sm font-medium text-green-600 hover:text-green-700">Chat Sekarang</a>
            </div>
          </div>
          <div class="flex items-start gap-4 p-6 border border-gray-100 dark:border-gray-700 rounded-2xl shadow-sm">
            <span class="inline-flex items-center justify-center w-12 h-12 rounded-full bg-blue-100 text-blue-600">
              <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path d="M4 4h16v2l-8 5-8-5V4zm0 4l8 5 8-5v10H4V8z" /></svg>
            </span>
            <div>
              <h3 class="text-lg font-semibold text-gray-900 dark:text-white">Email Tim AI</h3>
              <p class="mt-1 text-gray-600 dark:text-gray-400">Ideal untuk mengirimkan dokumen pendukung atau kebutuhan proyek detail.</p>
              <a href="mailto:myaicustom@gmail.com" class="mt-3 inline-flex items-center text-sm font-medium text-blue-600 hover:text-blue-700">myaicustom@gmail.com</a>
            </div>
          </div>
          <div class="flex items-start gap-4 p-6 border border-gray-100 dark:border-gray-700 rounded-2xl shadow-sm">
            <span class="inline-flex items-center justify-center w-12 h-12 rounded-full bg-purple-100 text-purple-600">
              <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 7a5 5 0 015 5v1h1a1 1 0 010 2h-1v2h-2v-2H9v2H7v-2H6a1 1 0 010-2h1v-1a5 5 0 015-5zm0 2a3 3 0 00-3 3v1h6v-1a3 3 0 00-3-3z" /></svg>
            </span>
            <div>
              <h3 class="text-lg font-semibold text-gray-900 dark:text-white">Jadwalkan Meeting</h3>
              <p class="mt-1 text-gray-600 dark:text-gray-400">Book sesi konsultasi via Google Meet/Zoom melalui kalender kami.</p>
              <a href="https://calendly.com/your-link" target="_blank" class="mt-3 inline-flex items-center text-sm font-medium text-purple-600 hover:text-purple-700">Pilih Slot Waktu</a>
            </div>
          </div>
        </div>
        <div class="rounded-2xl bg-gray-50 dark:bg-gray-800 p-6">
          <h3 class="text-lg font-semibold text-gray-900 dark:text-white">Jam Operasional</h3>
          <p class="mt-2 text-gray-600 dark:text-gray-400">Senin - Jumat, 09:00 - 17:00 WIB</p>
          <p class="mt-1 text-gray-600 dark:text-gray-400">Respon di luar jam kerja akan diproses di hari kerja berikutnya.</p>
        </div>
      </div>

      <!-- Contact Form -->
      <div class="bg-gray-50 dark:bg-gray-800 rounded-3xl shadow-xl p-8 lg:p-10">
        <h2 class="text-2xl font-semibold text-gray-900 dark:text-white">Formulir Konsultasi</h2>
        <p class="mt-2 text-sm text-gray-500 dark:text-gray-400">Ceritakan konteks bisnis Anda dan kami akan menghubungi dalam 1x24 jam kerja.</p>
        <form id="consultation-form" class="mt-8 space-y-6">
          <div>
            <label for="name" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Nama Lengkap</label>
            <input id="name" name="name" type="text" required class="mt-2 block w-full rounded-lg border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 focus:border-blue-500 focus:ring-blue-500" placeholder="Contoh: Putra Wijaya" />
          </div>
          <div>
            <label for="email" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Email</label>
            <input id="email" name="email" type="email" required class="mt-2 block w-full rounded-lg border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 focus:border-blue-500 focus:ring-blue-500" placeholder="nama@perusahaan.com" />
          </div>
          <div>
            <label for="phone" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Nomor WhatsApp</label>
            <input id="phone" name="phone" type="tel" class="mt-2 block w-full rounded-lg border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 focus:border-blue-500 focus:ring-blue-500" placeholder="08xxxxxxxxxx" />
          </div>
          <div>
            <label for="service" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Layanan yang diminati</label>
            <select id="service" name="service" class="mt-2 block w-full rounded-lg border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 focus:border-blue-500 focus:ring-blue-500">
              <option value="">Pilih salah satu</option>
              <option value="chatbot">Chatbot AI Custom</option>
              <option value="automation">Business Process Automation</option>
              <option value="analytics">AI Analytics & Insights</option>
              <option value="training">Training & Consultation</option>
              <option value="other">Lainnya</option>
            </select>
          </div>
          <div>
            <label for="message" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Ceritakan kebutuhan Anda</label>
            <textarea id="message" name="message" rows="4" class="mt-2 block w-full rounded-lg border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 focus:border-blue-500 focus:ring-blue-500" placeholder="Berikan gambaran singkat tentang bisnis Anda dan tantangan yang ingin diselesaikan."></textarea>
          </div>
          <button type="submit" class="w-full inline-flex justify-center items-center px-6 py-3 text-base font-semibold text-white bg-blue-600 rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:ring-offset-2 focus:ring-offset-gray-50 dark:focus:ring-offset-gray-800">Kirim Permintaan Konsultasi</button>
          <p class="text-xs text-gray-500 dark:text-gray-400">Dengan mengirimkan formulir ini Anda menyetujui kami menghubungi Anda terkait layanan AI my-aicustom.com.</p>
        </form>
      </div>
    </div>
  </section>

  <script is:inline define:vars={{ whatsappNumber }}>
    document.addEventListener('DOMContentLoaded', () => {
      const form = document.getElementById('consultation-form');
      if (!form) {
        return;
      }
      form.addEventListener('submit', (event) => {
        event.preventDefault();
        const formData = new FormData(form);
        const name = (formData.get('name') || '').toString().trim();
        const email = (formData.get('email') || '').toString().trim();
        const phone = (formData.get('phone') || '').toString().trim();
        const service = (formData.get('service') || '').toString().trim();
        const message = (formData.get('message') || '').toString().trim();

        const structuredMessage = [
          'Halo tim my-aicustom.com,',
          '',
          `Nama: ${name || '-'}`,
          `Email: ${email || '-'}`,
          `Nomor WhatsApp: ${phone || '-'}`,
          `Layanan: ${service || '-'}`,
          '',
          'Detail Kebutuhan:',
          message || '-'
        ].join('\n');

        const encodedMessage = encodeURIComponent(structuredMessage);
        const targetUrl = `https://wa.me/${whatsappNumber}?text=${encodedMessage}`;
        window.open(targetUrl, '_blank');
      });
    });
  </script>
</Layout>
